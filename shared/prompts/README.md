# Shared Prompts

Prompt templates live here so Android and iOS can maintain the same assistant behavior.

The app-specific runtime may embed these prompts at build time or load them from bundled assets, but both platforms should use the same source text unless the project leader approves a platform-specific variation.
