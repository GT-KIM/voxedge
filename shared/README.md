# Shared Contracts

This directory holds cross-platform contracts that Android and iOS must keep aligned.

- `config`: shared configuration names and expected value types.
- `mcp`: MCP-style messages exchanged between pipeline stages.
- `prompts`: prompt templates used before LLM inference.
