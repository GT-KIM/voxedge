# iOS Compile Pipeline

Planned LLM paths:

- ANEMLL conversion and benchmark.
- MLX conversion and benchmark.

Planned TTS path:

- Core ML Tools conversion to FP16 first.
- INT8 quantization only after performance evidence justifies it.

The default iOS runtime should be selected after throughput and integration comparison.
