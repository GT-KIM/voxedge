# Model Compile Tools

Use this directory for reproducible host-side model preparation workflows.

- Qualcomm Android pipeline: `qairt/`
- iOS pipeline: `ios/`

Model compile work should run in the user's local WSL environment unless a platform-specific tool requires otherwise.
